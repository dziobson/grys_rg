script:
  - id: on-press
    parameters:
      relay-nr: int
    then
      - if:
        condition:
          not:
            api.connected:
        then:
          - switch.toggle: relay_ !lambda return relay-nr
